global:
  scrape_interval: 15s
  external_labels:
    dc: {{ dc }}
    env: {{ env }}
    owner: {{ owner }}
    bare_metal: "true"

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets: ['localhost:9100']
